name: 'Delete packages'
description: 'Delete repo packages with the follwoing version'
inputs:
  package-token:  # id of input
    description: 'token'
    required: true
  package-version:  # id of input
    description: 'version'
    required: false
outputs:
  names: # id of output
    description: 'name of packages'
runs:
  using: 'node16'
  main: 'dist/deletePackage/index.js'
